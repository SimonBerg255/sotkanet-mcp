PORT=8000

# JWT authentication — copy to .env and set a strong random secret
# Generate a secret: python3 -c "import secrets; print(secrets.token_hex(32))"
MCP_SERVER_JWT_SECRET=change-me-min-32-chars-random-hex-string
MCP_SERVER_JWT_ISSUER=sotkanet-mcp
MCP_SERVER_JWT_AUDIENCE=intric
